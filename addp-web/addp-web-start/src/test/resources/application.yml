server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://59.110.234.213:3306/addp-web
    username: root
    password: kiss4400
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: true
    add-properties: true
  resources:
    #静态资源文件路径
    static-locations: classpath:/static/
  redis:
    host: 59.110.234.213
logging:
  level:
    com.dragon.study.spring.boot: DEBUG
  file: ./addp.log
